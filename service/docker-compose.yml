services:
  analytics:
    build: .
    container_name: analytics
    restart: unless-stopped
    volumes:
      - ${DATABASE_DIR:-./analytics-data}:/data
    ports:
      - "127.0.0.1:8082:3001"
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/data/${DATABASE_NAME:-analytics.db}
